<flow>
    <nodes>
        <node id="loanNodeComponent" name="a"/>
        <node id="sendLoanToBigDataNodeComponent" name="b"/>
        <node id="displayMapComponent" name="c"/>
    </nodes>

    <chain name="chain1">
        THEN(
        loanNodeComponent,
        node(sendLoanToBigDataNodeComponent).property("param1", 15).property("param2", "hello world"),
        sendLoanToBigDataNodeComponent,
        node(displayMapComponent).swap("newParam", ref("#body.d", "spel"))
        );
    </chain>
</flow>